node_modules
dist
coverage
*.log
.DS_Store

# Pipeline output artifacts (out/ dir is committed empty via .gitkeep)
out/*
!out/.gitkeep
out.mermaid
out.timeline

# Personal, machine-local Claude Code settings (committed counterpart: .claude/settings.json)
.claude/settings.local.json

# MCP server config — contains secrets (service account tokens)
.mcp.json
