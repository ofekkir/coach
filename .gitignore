node_modules
dist
coverage

# Ladle static catalog output (ladle build)
packages/app/build

# viz sub-project (workspace package)
viz/node_modules
viz/dist
src/graph/viz-dist/
*.log
.DS_Store

# Pipeline output artifacts (out/ dir is committed empty via .gitkeep)
out/*
!out/.gitkeep
out.mermaid
out.timeline

# Personal, machine-local Claude Code settings (committed counterpart: .claude/settings.json)
.claude/settings.local.json

# Claude Code worktrees (git worktrees created per session — not source files)
.claude/worktrees/

# MCP server config — contains secrets (service account tokens)
.mcp.json

# eslint-plugin-project-structure cache
projectStructure.cache.json
